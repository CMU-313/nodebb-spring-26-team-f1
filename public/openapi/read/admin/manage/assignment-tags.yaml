get:
  tags:
    - admin
  summary: Get assignment tag management page
  responses:
    "200":
      description: "A JSON object containing assignment tags data for the admin management page"
      content:
        application/json:
          schema:
            allOf:
              - type: object
                properties:
                  tags:
                    type: array
                    items:
                      type: object
              - $ref: ../../../components/schemas/CommonProps.yaml#/CommonProps
